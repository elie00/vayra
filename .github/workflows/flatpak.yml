name: flatpak

on:
  workflow_dispatch:
  pull_request:
    paths:
      - flatpak/**
      - src-tauri/**
      - vayra-core/**
      - src/**
      - public/**
      - index.html
      - vite.config.*
      - tsconfig*.json
      - package.json
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
      - .github/workflows/flatpak.yml

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - name: Install Flatpak tooling
        run: sudo apt-get update && sudo apt-get install -y flatpak flatpak-builder appstream desktop-file-utils ostree
      - name: Configure Flathub
        run: flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
      - name: Install pinned SDK
        run: >-
          flatpak install --user -y flathub
          org.gnome.Platform//49 org.gnome.Sdk//49
          org.freedesktop.Sdk.Extension.rust-stable//25.08
          org.freedesktop.Sdk.Extension.node22//25.08
      - name: Validate metadata
        run: |
          flatpak/update-flatpak.sh --check
          desktop-file-validate flatpak/site.vayra.Vayra.desktop
          appstreamcli validate --no-net flatpak/site.vayra.Vayra.metainfo.xml
          flatpak-builder --show-manifest flatpak/site.vayra.Vayra.yml >/dev/null
      - name: Build repository
        run: flatpak-builder --user --force-clean --state-dir=.flatpak-work/state --repo=.flatpak-work/repo .flatpak-work/build flatpak/site.vayra.Vayra.yml
      - name: Build single-file bundle
        run: flatpak build-bundle .flatpak-work/repo Harbor.flatpak site.vayra.Vayra
      - name: Validate bundle
        run: |
          ostree init --repo=.flatpak-work/validation-repo --mode=archive-z2
          flatpak build-import-bundle --update-appstream --no-update-summary .flatpak-work/validation-repo Harbor.flatpak
      - uses: actions/upload-artifact@v6
        with:
          name: Harbor-flatpak-x86_64
          path: Harbor.flatpak
          retention-days: 14
          if-no-files-found: error
