{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pip",
  "description": "PiP window (harbor-pip): app commands + event listen (core:default) and the drag region. No fs/http/dialog access.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["harbor-pip"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
