# The linked Vercel project has `site/` as its remote root. Patterns are
# therefore evaluated from that directory by Vercel: never exclude `public/`
# or `api/`, which are the site's deployable outputs.
node_modules/
dist/
src-tauri/
vara-broker/
flatpak/
supabase/
docs/
examples/
site/
